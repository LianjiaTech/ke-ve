const <%=depend%> = require("<%=depend%>");

module.exports = function(api) {
  api.registerPluginInfo({
    name: 've-cli-<%=depend%>',
    type: 'custom',
    depend: '<%=depend%>'
  });

  api.registerGenerate(function() {
    eval('<%=execute%>')
  })
}